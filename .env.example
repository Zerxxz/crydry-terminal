# Postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/crydry?schema=public"

# Optional chain providers (the app falls back to seeded DB data when missing)
ALCHEMY_API_KEY=""
ETHERSCAN_API_KEY=""
HELIUS_API_KEY=""

# WalletConnect Project ID (get free at https://cloud.walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""

# Public site URL (used for OG tags)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
